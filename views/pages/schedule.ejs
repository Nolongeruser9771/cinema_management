<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<div class="container">
  <h1>📅 Lập Lịch Chiếu Phim</h1>

  <% if (message) { %>
    <div class="alert alert-<%= message.type %>">
      <strong><%= message.type === 'success' ? '✓' : '⚠️' %></strong>
      <%= message.text %>
    </div>
  <% } %>

  <!-- Redesigned form with better layout and improved styling -->
  <div class="form-box">
    <h2>📝 Thêm Suất Chiếu Mới</h2>
    <form method="POST" action="/management/schedule/add">
      <div class="form-row">
        <div class="form-group">
          <label for="theaterId">Chọn rạp</label>
          <select id="theaterId" name="theaterId" required onchange="updateRoomOptions()">
            <option value="">-- Chọn rạp --</option>
            <% theaters.forEach(theater => { %>
              <option value="<%= theater.id %>"><%= theater.name %></option>
            <% }) %>
          </select>
        </div>

        <div class="form-group">
          <label for="movieId">Chọn phim</label>
          <select id="movieId" name="movieId" required>
            <option value="">-- Chọn phim --</option>
            <% movies.forEach(movie => { %>
              <option value="<%= movie.id %>"><%= movie.title %></option>
            <% }) %>
          </select>
        </div>

        <div class="form-group">
          <label for="roomId">Chọn phòng</label>
          <select id="roomId" name="roomId" required>
            <option value="">-- Chọn phòng --</option>
            <% rooms.forEach(room => { %>
              <option value="<%= room.id %>" data-theater="<%= room.theaterId %>"><%= room.name %> (<%= room.type %>, <%= room.seats %> ghế)</option>
            <% }) %>
          </select>
        </div>

        <div class="form-group">
          <label for="shiftId">Chọn ca</label>
          <select id="shiftId" name="shiftId" required>
            <option value="">-- Chọn ca --</option>
            <% shifts.forEach(shift => { %>
              <option value="<%= shift.id %>"><%= shift.name %> (<%= shift.time %>)</option>
            <% }) %>
          </select>
        </div>

        <div class="form-group">
          <label for="date">Chọn ngày</label>
          <input type="date" id="date" name="date" required>
        </div>

        <!-- Enhanced pricing section with icons -->
        <div class="form-group">
          <label for="standardPrice">Giá vé thường</label>
          <input type="number" id="standardPrice" name="standardPrice" required min="0" step="1000" placeholder="50000">
        </div>

        <div class="form-group">
          <label for="vipPrice">Giá vé VIP</label>
          <input type="number" id="vipPrice" name="vipPrice" required min="0" step="1000" placeholder="80000">
        </div>

        <div class="form-group">
          <button type="submit" class="btn btn-primary btn-block">+ Thêm suất chiếu</button>
        </div>
      </div>
    </form>
  </div>

  <!-- Improved results box with better visual hierarchy -->
  <div class="results-box">
    <h2>📊 Lịch Chiếu Hiện Tại</h2>
    <% if (showtimes.length > 0) { %>
    <div style="overflow-x: auto;">
      <table class="table">
        <thead>
          <tr>
            <th>Phim</th>
            <th>Rạp</th>
            <th>Phòng</th>
            <th>Ca chiếu</th>
            <th>Ngày</th>
            <th>Giá thường</th>
            <th>Giá VIP</th>
            <th>Thao tác</th>
          </tr>
        </thead>
        <tbody>
          <% showtimes.forEach(st => { %>
          <tr>
            <td><strong><%= st.movieTitle %></strong></td>
            <td><%= st.theaterName %></td>
            <td><%= st.roomName %></td>
            <td><%= st.shiftName %> (<%= st.shiftTime %>)</td>
            <td><%= st.date %></td>
            <td><span style="color: #34c759; font-weight: 600;"><%= st.standardPrice.toLocaleString('vi-VN') %> đ</span></td>
            <td><span style="color: #ffc107; font-weight: 600;"><%= st.vipPrice.toLocaleString('vi-VN') %> đ</span></td>
            <td>
              <form method="POST" action="/management/schedule/delete/<%= st.id %>" style="display: inline;">
                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc muốn xóa suất chiếu này?')">
                  Xóa
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } else { %>
      <p class="no-data">Chưa có suất chiếu nào được lập.</p>
    <% } %>
  </div>
</div>

<script>
  const theaterRoomsData = <%- JSON.stringify(rooms) %>;

  function updateRoomOptions() {
    const theaterId = document.getElementById('theaterId').value;
    const roomSelect = document.getElementById('roomId');
    
    roomSelect.innerHTML = '<option value="">-- Chọn phòng --</option>';
    
    if (theaterId) {
      theaterRoomsData
        .filter(r => r.theaterId === parseInt(theaterId))
        .forEach(room => {
          const option = document.createElement('option');
          option.value = room.id;
          option.textContent = `${room.name} (${room.type}, ${room.seats} ghế)`;
          roomSelect.appendChild(option);
        });
    }
  }
</script>

<%- include('../partials/footer') %>
