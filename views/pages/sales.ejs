<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<div class="container">
  <h1>🎫 Bán vé và Tra cứu Suất chiếu</h1>

  <!-- Redesigned search form with better UX -->
  <div class="search-box">
  <h2>🔍 Tra cứu Suất chiếu</h2>

  <form method="POST" action="/sales/search">

    <div class="form-row form-4">
      <div class="form-group">
        <label for="movieId">🎬 Tên phim</label>
        <select id="movieId" name="movieId">
          <option value="">-- Tất cả --</option>
          <% movies.forEach(movie => { %>
            <option value="<%= movie.id %>"><%= movie.title %></option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <label for="shiftId">⏰ Ca chiếu</label>
        <select id="shiftId" name="shiftId">
          <option value="">-- Tất cả --</option>
          <% shifts.forEach(shift => { %>
            <option value="<%= shift.id %>"><%= shift.name %> (<%= shift.time %>)</option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <label for="date">📅 Ngày chiếu</label>
        <input type="date" id="date" name="date">
      </div>

      <div class="form-group">
        <label for="theaterIds">🏢 Rạp chiếu</label>
        <!-- multiple nhưng để size=1 để chiều cao = dropdown thường -->
        <select id="theaterIds" name="theaterIds" multiple size="1">
          <% theaters.forEach(theater => { %>
            <option value="<%= theater.id %>"><%= theater.name %></option>
          <% }) %>
        </select>
      </div>
    </div>

    <button type="submit" class="btn btn-primary btn-search">Tìm kiếm</button>

  </form>
</div>


  <% if (showtimes.length > 0) { %>
  <!-- Enhanced results section with styling -->
  <div class="results-box">
    <h2>📊 Kết quả tìm kiếm (<%= showtimes.length %> suất chiếu)</h2>
    <div style="overflow-x: auto;">
      <table class="table">
        <thead>
          <tr>
            <th>Phim</th>
            <th>Rạp</th>
            <th>Phòng</th>
            <th>Ca chiếu</th>
            <th>Ngày</th>
            <th>Giá</th>
            <th>Thao tác</th>
          </tr>
        </thead>
        <tbody>
          <% showtimes.forEach(st => { %>
          <tr>
            <td><strong><%= st.movieTitle %></strong></td>
            <td><%= st.theaterName %></td>
            <td><%= st.roomName %></td>
            <td><%= st.shiftName %> (<%= st.shiftTime %>)</td>
            <td><strong><%= st.date %></strong></td>
            <td>
              <span style="color: #34c759; font-weight: 600;"><%= st.standardPrice.toLocaleString('vi-VN') %> đ</span> / 
              <span style="color: #ffc107; font-weight: 600;"><%= st.vipPrice.toLocaleString('vi-VN') %> đ</span>
            </td>
            <td>
              <button class="btn btn-success btn-sm" onclick="openSeatModal('<%= st.id %>')">
                Chọn ghế
              </button>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
  <% } else { %>
  <div class="results-box">
    <h2>📊 Kết quả tìm kiếm</h2>
    <p style="text-align:center; padding:20px; font-size:18px; color:#999;">
      Không có suất chiếu phù hợp.
    </p>
  </div>
<% } %>
</div>

<!-- Enhanced modal with improved pricing display -->
<div id="seatModal" class="modal">
  <div class="modal-content">
    <span class="close" onclick="closeSeatModal()">&times;</span>
    <h2 id="modalTitle">🎫 Chọn ghế</h2>
    <div id="seatInfo"></div>
    <div id="seatGrid" class="seat-grid"></div>
    <div class="modal-footer" style="flex-direction: column; text-align: left; border-top: none; margin-top: 24px;">
      <!-- Improved price breakdown display -->
      <div style="background: #f5f5f7; padding: 16px; border-radius: 6px; margin-bottom: 16px; width: 100%;">
        <p style="margin: 8px 0; font-size: 14px;"><strong>✓ Ghế đã chọn:</strong> <span id="selectedSeats" style="color: #0071e3;">Chưa chọn ghế nào</span></p>
        <div id="priceBreakdown" style="margin: 12px 0; font-size: 14px; white-space: pre-line; color: #666;"></div>
        <p style="margin: 12px 0; font-size: 16px;"><strong>💰 Tổng tiền: <span id="totalAmount" style="color: #34c759;">0</span> đ</strong></p>
      </div>
      <div style="display: flex; gap: 12px; width: 100%;">
        <button class="btn btn-primary" style="flex: 1;" onclick="sellTickets()">✓ Xác nhận bán vé</button>
        <button class="btn btn-secondary" style="flex: 1;" onclick="closeSeatModal()">✕ Hủy</button>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
